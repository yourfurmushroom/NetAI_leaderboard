import React from "react";

export default class UploadArea extends React.Component
{
    constructor(props)
    {
        super(props)
        this.state={

        }

        render()
        {
            return(
                <div>

                </div>
            )
        }
    }
}